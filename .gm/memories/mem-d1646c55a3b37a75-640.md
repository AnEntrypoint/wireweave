---
key: mem-d1646c55a3b37a75-640
ns: default
created: 1783330269125
updated: 1783330269125
---

gm-method: before serving a local static build for browser-witness, verify the port is not already bound by a stale/orphaned process from a prior session serving a DIFFERENT project's build output -- curl the title/known-content directly from the target port first, before dispatching the browser verb, since Windows can allow multiple listeners to bind the same port and route requests unpredictably to whichever process accepts first, producing a browser witness that silently validates the wrong page. Kill all matching stale server processes and rebind on a byte-verified-correct port before trusting any subsequent browser-verb result.
